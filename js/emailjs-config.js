/**
 * EmailJS config for admin reply-to-customer emails.
 * Create a template in EmailJS (see ADMIN_REPLY_EMAIL_SETUP.md) and fill in the values below.
 */
var EMAILJS_REPLY_CONFIG = {
  publicKey: '',      // EmailJS Account → API Keys → Public Key
  serviceId: '',     // Email Services → your Gmail (or other) service ID
  replyTemplateId: '' // Email Templates → "Admin Reply" template ID
};
